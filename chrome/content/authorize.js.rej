--- chrome/content/authorize.js
+++ chrome/content/authorize.js
@@ -23,7 +23,7 @@
 
 /**
  * \file authorize.js
- */
+*/
 var Cc = Components.classes;
 var Ci = Components.interfaces;
 var Cr = Components.results;
@@ -66,7 +66,17 @@
 
     if (deck.selectedPanel != this._browser) {
       doc.getElementById("username").value = this._service.username;
-      doc.getElementById("password").value = this._service.password;
+      var mainDoc = Cc["@mozilla.org/appshell/window-mediator;1"]
+                      .getService(Ci.nsIWindowMediator)
+                      .getMainWindow("Songbird:Main")
+                      .document;
+      var panelBinding = mainDoc.getElementById("soundcloudLoginPanel");
+      var password = mainDoc.getAnonymousElementByAttribute(panelBinding,
+                                                            "sbid",
+                                                            "password");
+      if (password)
+        doc.getElementById("password").value = password;
+
       deck.selectedPanel = this._browser;
       WindowUtils.sizeToContent(window);
       return;
